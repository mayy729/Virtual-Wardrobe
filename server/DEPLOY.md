# 云服务器部署指南

## 🚀 Render 部署（推荐，最简单）

### 步骤 1：准备 GitHub 仓库
确保你的代码已经推送到 GitHub：
```bash
git add .
git commit -m "准备部署"
git push
```

### 步骤 2：在 Render 创建账户
1. 访问 [https://render.com](https://render.com)
2. 点击 "Sign Up" 注册（可以使用 GitHub 账户登录）
3. 免费账户即可

### 步骤 3：创建 Web Service
1. 在 Render 控制台点击 "New +" → "Web Service"
2. 连接你的 GitHub 仓库：`mayy729/Virtual-Wardrobe`
3. 配置如下：
   - **Name**: `virtual-wardrobe-api`（任意名称）
   - **Region**: 选择离你最近的区域（如 Singapore）
   - **Branch**: `main` 或 `master`
   - **Root Directory**: `server` ⚠️ **重要：必须填写 `server`**
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: `Free`

4. 点击 "Create Web Service"

### 步骤 4：等待部署
- 首次部署需要 2-5 分钟
- 部署完成后，你会得到一个 URL，例如：`https://virtual-wardrobe-api.onrender.com`

### 步骤 5：配置前端
1. 访问 GitHub Pages：`https://mayy729.github.io/Virtual-Wardrobe/`
2. 点击 "⚙️ Settings"
3. 输入你的 Render URL（例如：`https://virtual-wardrobe-api.onrender.com`）
4. 点击 "测试连接"
5. 点击 "保存配置"

### 步骤 6：完成！
现在你可以在任何设备上访问 GitHub Pages，都能正常使用所有功能了！

---

## ⚠️ 注意事项

### Render 免费层限制
- **休眠机制**：15 分钟无活动后会自动休眠
- **首次唤醒**：休眠后首次请求需要等待 30-60 秒唤醒服务器
- **后续请求**：唤醒后速度正常
- **解决方案**：可以使用 [UptimeRobot](https://uptimerobot.com) 免费服务每 14 分钟 ping 一次，保持服务器活跃

### 数据持久化
- 数据文件（`wardrobe-data.json`, `outfits-data.json`）会保存在服务器上
- 如果重新部署，数据会保留（除非删除服务）

### 更新代码
- 每次推送到 GitHub，Render 会自动重新部署
- 或者可以在 Render 控制台手动触发部署

---

## 🔧 其他云服务选项

### Railway（推荐，不休眠）
1. 访问 [https://railway.app](https://railway.app)
2. 使用 GitHub 登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择仓库，Railway 会自动检测配置
5. 在设置中指定 Root Directory 为 `server`
6. 部署完成后获取 URL

**优点**：免费层不休眠，速度更快
**缺点**：需要信用卡验证（不扣费）

### Fly.io（全球部署）
1. 安装 Fly CLI：`iwr https://fly.io/install.ps1 -useb | iex`
2. 登录：`fly auth login`
3. 在 `server` 目录运行：`fly launch`
4. 按照提示完成配置

---

## 🆘 故障排除

### 问题 1：部署失败
- 检查 Root Directory 是否设置为 `server`
- 检查 `package.json` 中是否有 `start` 脚本
- 查看 Render 的构建日志

### 问题 2：CORS 错误
- 确保 `server.js` 中的 CORS 配置允许所有来源（已配置）
- 如果还有问题，检查 Render URL 是否正确

### 问题 3：服务器休眠
- 使用 UptimeRobot 保持服务器活跃
- 或升级到付费计划（不休眠）

---

## 📝 部署后检查清单

- [ ] Render 服务部署成功
- [ ] 访问 Render URL 看到 "API is running"
- [ ] 在前端配置中设置 Render URL
- [ ] 测试连接成功
- [ ] 测试上传功能
- [ ] 测试查看衣柜功能
- [ ] 在手机上测试（可选）

---

**需要帮助？** 查看 Render 文档：https://render.com/docs

